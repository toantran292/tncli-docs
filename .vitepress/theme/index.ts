import DefaultTheme from 'vitepress/theme'
import './custom.css'

// tncli docs reuse VitePress's default theme, restyled to the app's own
// color language (see custom.css) so the docs and the dashboard feel like
// one product.
export default DefaultTheme
